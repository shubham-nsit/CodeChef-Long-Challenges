#include<stdio.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n;
        scanf("%d",&n);
        int m=n,c=0,a;
        while(n>0)
        {
            a=sqrt(n);
            c++;
            n=n-a*a;
        }
        printf("%d\n",c);
    }
}
